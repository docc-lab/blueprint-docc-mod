// Blueprint: Auto-generated by the HTTP Plugin
package http

import (
	"fmt"
	"io"
	"errors"
	"net/http"
	"encoding/json"
	"context"
	"net/url"
)

type FancierGreeterService_HTTPClient struct {
	Client *http.Client
	ServerAddress string
}

func New_FancierGreeterService_HTTPClient(ctx context.Context, serverAddress string) (*FancierGreeterService_HTTPClient, error) {
	defaultRoundTripper := http.DefaultTransport
	defaultTransportPointer, ok := defaultRoundTripper.(*http.Transport)
	if !ok {
		return nil, errors.New("defaultRoundTripper not an *http.Transport")
	}
	defaultTransport := *defaultTransportPointer // dereference it to get a copy of the struct that the pointer points to
	defaultTransport.MaxIdleConns = 60000
	defaultTransport.MaxIdleConnsPerHost = 60000
	defaultTransport.MaxConnsPerHost = 10000
	client := &http.Client{
		Transport: &defaultTransport,
	}
	c := &FancierGreeterService_HTTPClient{}
	c.Client = client
	c.ServerAddress = "http://" + serverAddress
	return c, nil
}


func (client *FancierGreeterService_HTTPClient) FarewellWithEmotion(ctx context.Context, name string, emotion string, traceCtx string) (ret0 string, err error) {
	vals := url.Values{}
	
	vals.Add("name", name)
	
	vals.Add("emotion", emotion)
	
	vals.Add("traceCtx", traceCtx)
	

	encoded_url, err := url.Parse(client.ServerAddress + "/FarewellWithEmotion")
	if err != nil {
		return
	}
	encoded_url.RawQuery = vals.Encode()

	resp, err := client.Client.Get(encoded_url.String())
	if err != nil {
		return
	}
	defer resp.Body.Close()
	statusOk := resp.StatusCode >= 200 && resp.StatusCode < 300
	if !statusOk {
		err = fmt.Errorf("StatusCode was %d", resp.StatusCode)
		return
	}
	response := struct {
		
		Ret0 string
		
	}{}
	resp_bytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return
	}
	err = json.Unmarshal(resp_bytes, &response)
	if err != nil {
		return
	}
	
	ret0 = response.Ret0
	
	return
}

func (client *FancierGreeterService_HTTPClient) GreetWithTitle(ctx context.Context, name string, title string, traceCtx string) (ret0 string, err error) {
	vals := url.Values{}
	
	vals.Add("name", name)
	
	vals.Add("title", title)
	
	vals.Add("traceCtx", traceCtx)
	

	encoded_url, err := url.Parse(client.ServerAddress + "/GreetWithTitle")
	if err != nil {
		return
	}
	encoded_url.RawQuery = vals.Encode()

	resp, err := client.Client.Get(encoded_url.String())
	if err != nil {
		return
	}
	defer resp.Body.Close()
	statusOk := resp.StatusCode >= 200 && resp.StatusCode < 300
	if !statusOk {
		err = fmt.Errorf("StatusCode was %d", resp.StatusCode)
		return
	}
	response := struct {
		
		Ret0 string
		
	}{}
	resp_bytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return
	}
	err = json.Unmarshal(resp_bytes, &response)
	if err != nil {
		return
	}
	
	ret0 = response.Ret0
	
	return
}

