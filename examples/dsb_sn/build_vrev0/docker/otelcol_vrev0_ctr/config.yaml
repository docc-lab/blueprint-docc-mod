exporters:
    debug:
        verbosity: detailed
    otlp:
        endpoint: ${JAEGER_VREV0_OTLP_DIAL_ADDR}
        retry_on_failure:
            enabled: true
            initial_interval: 5s
            max_elapsed_time: 0
            max_interval: 30s
        sending_queue:
            block_on_overflow: false
            enabled: true
            queue_size: 100000
        tls:
            insecure: true
processors:
    batch: null
    memory_limiter:
        check_interval: 1s
        limit_percentage: 80
        spike_limit_percentage: 20
receivers:
    otlp:
        protocols:
            grpc:
                endpoint: 0.0.0.0:4317
            http:
                endpoint: 0.0.0.0:4318
service:
    pipelines:
        traces:
            exporters:
                - debug
                - otlp
            processors:
                - memory_limiter
                - batch
            receivers:
                - otlp
    telemetry:
        logs:
            level: DEBUG
