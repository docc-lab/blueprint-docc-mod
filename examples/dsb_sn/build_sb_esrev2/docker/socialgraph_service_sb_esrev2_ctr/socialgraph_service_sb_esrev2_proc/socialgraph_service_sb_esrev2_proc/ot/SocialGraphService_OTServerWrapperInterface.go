// Blueprint: Auto-generated by OpenTelemetry Plugin
package ot

import (
	"context"
	"encoding/base64"
	"encoding/binary"
	"strconv"
	"strings"
	"sync"
	"sync/atomic"

	"github.com/blueprint-uservices/blueprint/examples/dsb_sn/workflow/socialnetwork"
	"github.com/blueprint-uservices/blueprint/runtime/core/backend"
	"go.opentelemetry.io/otel/attribute"
	trace2 "go.opentelemetry.io/otel/sdk/trace"
	"go.opentelemetry.io/otel/trace"
)

type SocialGraphService_OTServerWrapperInterface interface {
	Follow(ctx context.Context, reqID int64, userID int64, followeeID int64, traceCtx string) error
	FollowWithUsername(ctx context.Context, reqID int64, userUsername string, followeeUsername string, traceCtx string) error
	GetFollowees(ctx context.Context, reqID int64, userID int64, traceCtx string) ([]int64, error)
	GetFollowers(ctx context.Context, reqID int64, userID int64, traceCtx string) ([]int64, error)
	InsertUser(ctx context.Context, reqID int64, userID int64, traceCtx string) error
	Unfollow(ctx context.Context, reqID int64, userID int64, followeeID int64, traceCtx string) error
	UnfollowWithUsername(ctx context.Context, reqID int64, userUsername string, followeeUsername string, traceCtx string) error
}

type SocialGraphService_OTServerWrapper struct {
	Service    socialnetwork.SocialGraphService
	CollClient backend.Tracer
}

func New_SocialGraphService_OTServerWrapper(ctx context.Context, service socialnetwork.SocialGraphService, coll_client backend.Tracer) (*SocialGraphService_OTServerWrapper, error) {
	handler := &SocialGraphService_OTServerWrapper{}
	handler.Service = service
	handler.CollClient = coll_client
	return handler, nil
}

func (handler *SocialGraphService_OTServerWrapper) Follow(ctx context.Context, reqID int64, userID int64, followeeID int64, traceCtx string) (err error) {
	var baggage map[string]string
	if traceCtx != "" {
		span_ctx_config, upstreamBaggage, _ := backend.GetSpanContext(traceCtx)
		span_ctx := trace.NewSpanContext(span_ctx_config)
		ctx = trace.ContextWithRemoteSpanContext(ctx, span_ctx)

		// Set baggage in context for span processor to read
		if upstreamBaggage != nil {
			baggage = upstreamBaggage
			ctx = backend.SetBaggageInContext(ctx, baggage)
		}
	}

	tp, _ := handler.CollClient.GetTracerProvider(ctx)
	tr := tp.Tracer("SocialGraphService")
	ctx, span := tr.Start(ctx, "SocialGraphServiceServer_Follow", trace.WithSpanKind(trace.SpanKindServer))
	defer span.End()

	// Extract new baggage from span attributes by casting to ReadWriteSpan
	if baggage == nil {
		baggage = make(map[string]string)
	}
	if rwSpan, ok := span.(trace2.ReadWriteSpan); ok {
		for _, attr := range rwSpan.Attributes() {
			if strings.HasPrefix(string(attr.Key), "__bag.") {
				key := strings.TrimPrefix(string(attr.Key), "__bag.")
				// Convert value to string for baggage based on attribute type
				switch attr.Value.Type() {
				case attribute.INT64:
					baggage[key] = strconv.FormatInt(attr.Value.AsInt64(), 10)
				case attribute.STRING:
					baggage[key] = attr.Value.AsString()
				default:
					baggage[key] = attr.Value.AsString()
				}
			} else if strings.HasPrefix(string(attr.Key), "__bagdel.") {
				key := strings.TrimPrefix(string(attr.Key), "__bagdel.")
				delete(baggage, key)
			}
		}
	}

	// Update context with merged baggage BEFORE calling the service
	if baggage != nil {
		ctx = backend.SetBaggageInContext(ctx, baggage)
	}

	eventCount := atomic.Uint64{}
	ctx = context.WithValue(ctx, "eventCount", &eventCount)

	// End events tracking structures. Matches the bridges Go simulator's
	// parentEEAcc: a []int slice of sibling start-seqs in the order in
	// which they ENDED. Client interceptors append each child's startSeq
	// to this slice when the child's call returns. At server-OnEnd we
	// varint-encode the slice (dropping the last entry per simulator
	// semantics; the last sibling's end is implicit at trace
	// reconstruction) and stash the bytes on a per-span attribute that
	// the SB processor reads at its own OnEnd hook.
	childrenMutex := sync.Mutex{}
	endEvents := []int(nil)
	ctx = context.WithValue(ctx, "endEvents", &endEvents)
	ctx = context.WithValue(ctx, "childrenMutex", &childrenMutex)

	err = handler.Service.Follow(ctx, reqID, userID, followeeID)
	if err != nil {
		span.RecordError(err)
	}

	span.SetAttributes(attribute.Int("eventCount", int(eventCount.Load())))
	// Encode the end-event seqs as varint bytes for the SB processor.
	// Format: varint(count) || count*varint(seq). The SB processor's
	// OnEnd hook prepends traceID+depth to form a full DEE triple.
	// Drop the last entry — its end is implicit at reconstruction time
	// (mirrors the simulator's kept = rem[:len(rem)-1]).
	if n := len(endEvents); n > 0 {
		kept := endEvents[:n-1]
		buf := make([]byte, 0, 8+5*len(kept))
		buf = binary.AppendUvarint(buf, uint64(len(kept)))
		for _, s := range kept {
			buf = binary.AppendUvarint(buf, uint64(s))
		}
		// Base64 the bytes because OTel attributes don't expose a
		// native []byte type; this is the SDK's only string round
		// trip on the DEE path, paid once per server-OnEnd.
		span.SetAttributes(attribute.String("remEndEvents", base64.RawURLEncoding.EncodeToString(buf)))
	}

	return
}

func (handler *SocialGraphService_OTServerWrapper) FollowWithUsername(ctx context.Context, reqID int64, userUsername string, followeeUsername string, traceCtx string) (err error) {
	var baggage map[string]string
	if traceCtx != "" {
		span_ctx_config, upstreamBaggage, _ := backend.GetSpanContext(traceCtx)
		span_ctx := trace.NewSpanContext(span_ctx_config)
		ctx = trace.ContextWithRemoteSpanContext(ctx, span_ctx)

		// Set baggage in context for span processor to read
		if upstreamBaggage != nil {
			baggage = upstreamBaggage
			ctx = backend.SetBaggageInContext(ctx, baggage)
		}
	}

	tp, _ := handler.CollClient.GetTracerProvider(ctx)
	tr := tp.Tracer("SocialGraphService")
	ctx, span := tr.Start(ctx, "SocialGraphServiceServer_FollowWithUsername", trace.WithSpanKind(trace.SpanKindServer))
	defer span.End()

	// Extract new baggage from span attributes by casting to ReadWriteSpan
	if baggage == nil {
		baggage = make(map[string]string)
	}
	if rwSpan, ok := span.(trace2.ReadWriteSpan); ok {
		for _, attr := range rwSpan.Attributes() {
			if strings.HasPrefix(string(attr.Key), "__bag.") {
				key := strings.TrimPrefix(string(attr.Key), "__bag.")
				// Convert value to string for baggage based on attribute type
				switch attr.Value.Type() {
				case attribute.INT64:
					baggage[key] = strconv.FormatInt(attr.Value.AsInt64(), 10)
				case attribute.STRING:
					baggage[key] = attr.Value.AsString()
				default:
					baggage[key] = attr.Value.AsString()
				}
			} else if strings.HasPrefix(string(attr.Key), "__bagdel.") {
				key := strings.TrimPrefix(string(attr.Key), "__bagdel.")
				delete(baggage, key)
			}
		}
	}

	// Update context with merged baggage BEFORE calling the service
	if baggage != nil {
		ctx = backend.SetBaggageInContext(ctx, baggage)
	}

	eventCount := atomic.Uint64{}
	ctx = context.WithValue(ctx, "eventCount", &eventCount)

	// End events tracking structures. Matches the bridges Go simulator's
	// parentEEAcc: a []int slice of sibling start-seqs in the order in
	// which they ENDED. Client interceptors append each child's startSeq
	// to this slice when the child's call returns. At server-OnEnd we
	// varint-encode the slice (dropping the last entry per simulator
	// semantics; the last sibling's end is implicit at trace
	// reconstruction) and stash the bytes on a per-span attribute that
	// the SB processor reads at its own OnEnd hook.
	childrenMutex := sync.Mutex{}
	endEvents := []int(nil)
	ctx = context.WithValue(ctx, "endEvents", &endEvents)
	ctx = context.WithValue(ctx, "childrenMutex", &childrenMutex)

	err = handler.Service.FollowWithUsername(ctx, reqID, userUsername, followeeUsername)
	if err != nil {
		span.RecordError(err)
	}

	span.SetAttributes(attribute.Int("eventCount", int(eventCount.Load())))
	// Encode the end-event seqs as varint bytes for the SB processor.
	// Format: varint(count) || count*varint(seq). The SB processor's
	// OnEnd hook prepends traceID+depth to form a full DEE triple.
	// Drop the last entry — its end is implicit at reconstruction time
	// (mirrors the simulator's kept = rem[:len(rem)-1]).
	if n := len(endEvents); n > 0 {
		kept := endEvents[:n-1]
		buf := make([]byte, 0, 8+5*len(kept))
		buf = binary.AppendUvarint(buf, uint64(len(kept)))
		for _, s := range kept {
			buf = binary.AppendUvarint(buf, uint64(s))
		}
		// Base64 the bytes because OTel attributes don't expose a
		// native []byte type; this is the SDK's only string round
		// trip on the DEE path, paid once per server-OnEnd.
		span.SetAttributes(attribute.String("remEndEvents", base64.RawURLEncoding.EncodeToString(buf)))
	}

	return
}

func (handler *SocialGraphService_OTServerWrapper) GetFollowees(ctx context.Context, reqID int64, userID int64, traceCtx string) (ret0 []int64, err error) {
	var baggage map[string]string
	if traceCtx != "" {
		span_ctx_config, upstreamBaggage, _ := backend.GetSpanContext(traceCtx)
		span_ctx := trace.NewSpanContext(span_ctx_config)
		ctx = trace.ContextWithRemoteSpanContext(ctx, span_ctx)

		// Set baggage in context for span processor to read
		if upstreamBaggage != nil {
			baggage = upstreamBaggage
			ctx = backend.SetBaggageInContext(ctx, baggage)
		}
	}

	tp, _ := handler.CollClient.GetTracerProvider(ctx)
	tr := tp.Tracer("SocialGraphService")
	ctx, span := tr.Start(ctx, "SocialGraphServiceServer_GetFollowees", trace.WithSpanKind(trace.SpanKindServer))
	defer span.End()

	// Extract new baggage from span attributes by casting to ReadWriteSpan
	if baggage == nil {
		baggage = make(map[string]string)
	}
	if rwSpan, ok := span.(trace2.ReadWriteSpan); ok {
		for _, attr := range rwSpan.Attributes() {
			if strings.HasPrefix(string(attr.Key), "__bag.") {
				key := strings.TrimPrefix(string(attr.Key), "__bag.")
				// Convert value to string for baggage based on attribute type
				switch attr.Value.Type() {
				case attribute.INT64:
					baggage[key] = strconv.FormatInt(attr.Value.AsInt64(), 10)
				case attribute.STRING:
					baggage[key] = attr.Value.AsString()
				default:
					baggage[key] = attr.Value.AsString()
				}
			} else if strings.HasPrefix(string(attr.Key), "__bagdel.") {
				key := strings.TrimPrefix(string(attr.Key), "__bagdel.")
				delete(baggage, key)
			}
		}
	}

	// Update context with merged baggage BEFORE calling the service
	if baggage != nil {
		ctx = backend.SetBaggageInContext(ctx, baggage)
	}

	eventCount := atomic.Uint64{}
	ctx = context.WithValue(ctx, "eventCount", &eventCount)

	// End events tracking structures. Matches the bridges Go simulator's
	// parentEEAcc: a []int slice of sibling start-seqs in the order in
	// which they ENDED. Client interceptors append each child's startSeq
	// to this slice when the child's call returns. At server-OnEnd we
	// varint-encode the slice (dropping the last entry per simulator
	// semantics; the last sibling's end is implicit at trace
	// reconstruction) and stash the bytes on a per-span attribute that
	// the SB processor reads at its own OnEnd hook.
	childrenMutex := sync.Mutex{}
	endEvents := []int(nil)
	ctx = context.WithValue(ctx, "endEvents", &endEvents)
	ctx = context.WithValue(ctx, "childrenMutex", &childrenMutex)

	ret0, err = handler.Service.GetFollowees(ctx, reqID, userID)
	if err != nil {
		span.RecordError(err)
	}

	span.SetAttributes(attribute.Int("eventCount", int(eventCount.Load())))
	// Encode the end-event seqs as varint bytes for the SB processor.
	// Format: varint(count) || count*varint(seq). The SB processor's
	// OnEnd hook prepends traceID+depth to form a full DEE triple.
	// Drop the last entry — its end is implicit at reconstruction time
	// (mirrors the simulator's kept = rem[:len(rem)-1]).
	if n := len(endEvents); n > 0 {
		kept := endEvents[:n-1]
		buf := make([]byte, 0, 8+5*len(kept))
		buf = binary.AppendUvarint(buf, uint64(len(kept)))
		for _, s := range kept {
			buf = binary.AppendUvarint(buf, uint64(s))
		}
		// Base64 the bytes because OTel attributes don't expose a
		// native []byte type; this is the SDK's only string round
		// trip on the DEE path, paid once per server-OnEnd.
		span.SetAttributes(attribute.String("remEndEvents", base64.RawURLEncoding.EncodeToString(buf)))
	}

	return
}

func (handler *SocialGraphService_OTServerWrapper) GetFollowers(ctx context.Context, reqID int64, userID int64, traceCtx string) (ret0 []int64, err error) {
	var baggage map[string]string
	if traceCtx != "" {
		span_ctx_config, upstreamBaggage, _ := backend.GetSpanContext(traceCtx)
		span_ctx := trace.NewSpanContext(span_ctx_config)
		ctx = trace.ContextWithRemoteSpanContext(ctx, span_ctx)

		// Set baggage in context for span processor to read
		if upstreamBaggage != nil {
			baggage = upstreamBaggage
			ctx = backend.SetBaggageInContext(ctx, baggage)
		}
	}

	tp, _ := handler.CollClient.GetTracerProvider(ctx)
	tr := tp.Tracer("SocialGraphService")
	ctx, span := tr.Start(ctx, "SocialGraphServiceServer_GetFollowers", trace.WithSpanKind(trace.SpanKindServer))
	defer span.End()

	// Extract new baggage from span attributes by casting to ReadWriteSpan
	if baggage == nil {
		baggage = make(map[string]string)
	}
	if rwSpan, ok := span.(trace2.ReadWriteSpan); ok {
		for _, attr := range rwSpan.Attributes() {
			if strings.HasPrefix(string(attr.Key), "__bag.") {
				key := strings.TrimPrefix(string(attr.Key), "__bag.")
				// Convert value to string for baggage based on attribute type
				switch attr.Value.Type() {
				case attribute.INT64:
					baggage[key] = strconv.FormatInt(attr.Value.AsInt64(), 10)
				case attribute.STRING:
					baggage[key] = attr.Value.AsString()
				default:
					baggage[key] = attr.Value.AsString()
				}
			} else if strings.HasPrefix(string(attr.Key), "__bagdel.") {
				key := strings.TrimPrefix(string(attr.Key), "__bagdel.")
				delete(baggage, key)
			}
		}
	}

	// Update context with merged baggage BEFORE calling the service
	if baggage != nil {
		ctx = backend.SetBaggageInContext(ctx, baggage)
	}

	eventCount := atomic.Uint64{}
	ctx = context.WithValue(ctx, "eventCount", &eventCount)

	// End events tracking structures. Matches the bridges Go simulator's
	// parentEEAcc: a []int slice of sibling start-seqs in the order in
	// which they ENDED. Client interceptors append each child's startSeq
	// to this slice when the child's call returns. At server-OnEnd we
	// varint-encode the slice (dropping the last entry per simulator
	// semantics; the last sibling's end is implicit at trace
	// reconstruction) and stash the bytes on a per-span attribute that
	// the SB processor reads at its own OnEnd hook.
	childrenMutex := sync.Mutex{}
	endEvents := []int(nil)
	ctx = context.WithValue(ctx, "endEvents", &endEvents)
	ctx = context.WithValue(ctx, "childrenMutex", &childrenMutex)

	ret0, err = handler.Service.GetFollowers(ctx, reqID, userID)
	if err != nil {
		span.RecordError(err)
	}

	span.SetAttributes(attribute.Int("eventCount", int(eventCount.Load())))
	// Encode the end-event seqs as varint bytes for the SB processor.
	// Format: varint(count) || count*varint(seq). The SB processor's
	// OnEnd hook prepends traceID+depth to form a full DEE triple.
	// Drop the last entry — its end is implicit at reconstruction time
	// (mirrors the simulator's kept = rem[:len(rem)-1]).
	if n := len(endEvents); n > 0 {
		kept := endEvents[:n-1]
		buf := make([]byte, 0, 8+5*len(kept))
		buf = binary.AppendUvarint(buf, uint64(len(kept)))
		for _, s := range kept {
			buf = binary.AppendUvarint(buf, uint64(s))
		}
		// Base64 the bytes because OTel attributes don't expose a
		// native []byte type; this is the SDK's only string round
		// trip on the DEE path, paid once per server-OnEnd.
		span.SetAttributes(attribute.String("remEndEvents", base64.RawURLEncoding.EncodeToString(buf)))
	}

	return
}

func (handler *SocialGraphService_OTServerWrapper) InsertUser(ctx context.Context, reqID int64, userID int64, traceCtx string) (err error) {
	var baggage map[string]string
	if traceCtx != "" {
		span_ctx_config, upstreamBaggage, _ := backend.GetSpanContext(traceCtx)
		span_ctx := trace.NewSpanContext(span_ctx_config)
		ctx = trace.ContextWithRemoteSpanContext(ctx, span_ctx)

		// Set baggage in context for span processor to read
		if upstreamBaggage != nil {
			baggage = upstreamBaggage
			ctx = backend.SetBaggageInContext(ctx, baggage)
		}
	}

	tp, _ := handler.CollClient.GetTracerProvider(ctx)
	tr := tp.Tracer("SocialGraphService")
	ctx, span := tr.Start(ctx, "SocialGraphServiceServer_InsertUser", trace.WithSpanKind(trace.SpanKindServer))
	defer span.End()

	// Extract new baggage from span attributes by casting to ReadWriteSpan
	if baggage == nil {
		baggage = make(map[string]string)
	}
	if rwSpan, ok := span.(trace2.ReadWriteSpan); ok {
		for _, attr := range rwSpan.Attributes() {
			if strings.HasPrefix(string(attr.Key), "__bag.") {
				key := strings.TrimPrefix(string(attr.Key), "__bag.")
				// Convert value to string for baggage based on attribute type
				switch attr.Value.Type() {
				case attribute.INT64:
					baggage[key] = strconv.FormatInt(attr.Value.AsInt64(), 10)
				case attribute.STRING:
					baggage[key] = attr.Value.AsString()
				default:
					baggage[key] = attr.Value.AsString()
				}
			} else if strings.HasPrefix(string(attr.Key), "__bagdel.") {
				key := strings.TrimPrefix(string(attr.Key), "__bagdel.")
				delete(baggage, key)
			}
		}
	}

	// Update context with merged baggage BEFORE calling the service
	if baggage != nil {
		ctx = backend.SetBaggageInContext(ctx, baggage)
	}

	eventCount := atomic.Uint64{}
	ctx = context.WithValue(ctx, "eventCount", &eventCount)

	// End events tracking structures. Matches the bridges Go simulator's
	// parentEEAcc: a []int slice of sibling start-seqs in the order in
	// which they ENDED. Client interceptors append each child's startSeq
	// to this slice when the child's call returns. At server-OnEnd we
	// varint-encode the slice (dropping the last entry per simulator
	// semantics; the last sibling's end is implicit at trace
	// reconstruction) and stash the bytes on a per-span attribute that
	// the SB processor reads at its own OnEnd hook.
	childrenMutex := sync.Mutex{}
	endEvents := []int(nil)
	ctx = context.WithValue(ctx, "endEvents", &endEvents)
	ctx = context.WithValue(ctx, "childrenMutex", &childrenMutex)

	err = handler.Service.InsertUser(ctx, reqID, userID)
	if err != nil {
		span.RecordError(err)
	}

	span.SetAttributes(attribute.Int("eventCount", int(eventCount.Load())))
	// Encode the end-event seqs as varint bytes for the SB processor.
	// Format: varint(count) || count*varint(seq). The SB processor's
	// OnEnd hook prepends traceID+depth to form a full DEE triple.
	// Drop the last entry — its end is implicit at reconstruction time
	// (mirrors the simulator's kept = rem[:len(rem)-1]).
	if n := len(endEvents); n > 0 {
		kept := endEvents[:n-1]
		buf := make([]byte, 0, 8+5*len(kept))
		buf = binary.AppendUvarint(buf, uint64(len(kept)))
		for _, s := range kept {
			buf = binary.AppendUvarint(buf, uint64(s))
		}
		// Base64 the bytes because OTel attributes don't expose a
		// native []byte type; this is the SDK's only string round
		// trip on the DEE path, paid once per server-OnEnd.
		span.SetAttributes(attribute.String("remEndEvents", base64.RawURLEncoding.EncodeToString(buf)))
	}

	return
}

func (handler *SocialGraphService_OTServerWrapper) Unfollow(ctx context.Context, reqID int64, userID int64, followeeID int64, traceCtx string) (err error) {
	var baggage map[string]string
	if traceCtx != "" {
		span_ctx_config, upstreamBaggage, _ := backend.GetSpanContext(traceCtx)
		span_ctx := trace.NewSpanContext(span_ctx_config)
		ctx = trace.ContextWithRemoteSpanContext(ctx, span_ctx)

		// Set baggage in context for span processor to read
		if upstreamBaggage != nil {
			baggage = upstreamBaggage
			ctx = backend.SetBaggageInContext(ctx, baggage)
		}
	}

	tp, _ := handler.CollClient.GetTracerProvider(ctx)
	tr := tp.Tracer("SocialGraphService")
	ctx, span := tr.Start(ctx, "SocialGraphServiceServer_Unfollow", trace.WithSpanKind(trace.SpanKindServer))
	defer span.End()

	// Extract new baggage from span attributes by casting to ReadWriteSpan
	if baggage == nil {
		baggage = make(map[string]string)
	}
	if rwSpan, ok := span.(trace2.ReadWriteSpan); ok {
		for _, attr := range rwSpan.Attributes() {
			if strings.HasPrefix(string(attr.Key), "__bag.") {
				key := strings.TrimPrefix(string(attr.Key), "__bag.")
				// Convert value to string for baggage based on attribute type
				switch attr.Value.Type() {
				case attribute.INT64:
					baggage[key] = strconv.FormatInt(attr.Value.AsInt64(), 10)
				case attribute.STRING:
					baggage[key] = attr.Value.AsString()
				default:
					baggage[key] = attr.Value.AsString()
				}
			} else if strings.HasPrefix(string(attr.Key), "__bagdel.") {
				key := strings.TrimPrefix(string(attr.Key), "__bagdel.")
				delete(baggage, key)
			}
		}
	}

	// Update context with merged baggage BEFORE calling the service
	if baggage != nil {
		ctx = backend.SetBaggageInContext(ctx, baggage)
	}

	eventCount := atomic.Uint64{}
	ctx = context.WithValue(ctx, "eventCount", &eventCount)

	// End events tracking structures. Matches the bridges Go simulator's
	// parentEEAcc: a []int slice of sibling start-seqs in the order in
	// which they ENDED. Client interceptors append each child's startSeq
	// to this slice when the child's call returns. At server-OnEnd we
	// varint-encode the slice (dropping the last entry per simulator
	// semantics; the last sibling's end is implicit at trace
	// reconstruction) and stash the bytes on a per-span attribute that
	// the SB processor reads at its own OnEnd hook.
	childrenMutex := sync.Mutex{}
	endEvents := []int(nil)
	ctx = context.WithValue(ctx, "endEvents", &endEvents)
	ctx = context.WithValue(ctx, "childrenMutex", &childrenMutex)

	err = handler.Service.Unfollow(ctx, reqID, userID, followeeID)
	if err != nil {
		span.RecordError(err)
	}

	span.SetAttributes(attribute.Int("eventCount", int(eventCount.Load())))
	// Encode the end-event seqs as varint bytes for the SB processor.
	// Format: varint(count) || count*varint(seq). The SB processor's
	// OnEnd hook prepends traceID+depth to form a full DEE triple.
	// Drop the last entry — its end is implicit at reconstruction time
	// (mirrors the simulator's kept = rem[:len(rem)-1]).
	if n := len(endEvents); n > 0 {
		kept := endEvents[:n-1]
		buf := make([]byte, 0, 8+5*len(kept))
		buf = binary.AppendUvarint(buf, uint64(len(kept)))
		for _, s := range kept {
			buf = binary.AppendUvarint(buf, uint64(s))
		}
		// Base64 the bytes because OTel attributes don't expose a
		// native []byte type; this is the SDK's only string round
		// trip on the DEE path, paid once per server-OnEnd.
		span.SetAttributes(attribute.String("remEndEvents", base64.RawURLEncoding.EncodeToString(buf)))
	}

	return
}

func (handler *SocialGraphService_OTServerWrapper) UnfollowWithUsername(ctx context.Context, reqID int64, userUsername string, followeeUsername string, traceCtx string) (err error) {
	var baggage map[string]string
	if traceCtx != "" {
		span_ctx_config, upstreamBaggage, _ := backend.GetSpanContext(traceCtx)
		span_ctx := trace.NewSpanContext(span_ctx_config)
		ctx = trace.ContextWithRemoteSpanContext(ctx, span_ctx)

		// Set baggage in context for span processor to read
		if upstreamBaggage != nil {
			baggage = upstreamBaggage
			ctx = backend.SetBaggageInContext(ctx, baggage)
		}
	}

	tp, _ := handler.CollClient.GetTracerProvider(ctx)
	tr := tp.Tracer("SocialGraphService")
	ctx, span := tr.Start(ctx, "SocialGraphServiceServer_UnfollowWithUsername", trace.WithSpanKind(trace.SpanKindServer))
	defer span.End()

	// Extract new baggage from span attributes by casting to ReadWriteSpan
	if baggage == nil {
		baggage = make(map[string]string)
	}
	if rwSpan, ok := span.(trace2.ReadWriteSpan); ok {
		for _, attr := range rwSpan.Attributes() {
			if strings.HasPrefix(string(attr.Key), "__bag.") {
				key := strings.TrimPrefix(string(attr.Key), "__bag.")
				// Convert value to string for baggage based on attribute type
				switch attr.Value.Type() {
				case attribute.INT64:
					baggage[key] = strconv.FormatInt(attr.Value.AsInt64(), 10)
				case attribute.STRING:
					baggage[key] = attr.Value.AsString()
				default:
					baggage[key] = attr.Value.AsString()
				}
			} else if strings.HasPrefix(string(attr.Key), "__bagdel.") {
				key := strings.TrimPrefix(string(attr.Key), "__bagdel.")
				delete(baggage, key)
			}
		}
	}

	// Update context with merged baggage BEFORE calling the service
	if baggage != nil {
		ctx = backend.SetBaggageInContext(ctx, baggage)
	}

	eventCount := atomic.Uint64{}
	ctx = context.WithValue(ctx, "eventCount", &eventCount)

	// End events tracking structures. Matches the bridges Go simulator's
	// parentEEAcc: a []int slice of sibling start-seqs in the order in
	// which they ENDED. Client interceptors append each child's startSeq
	// to this slice when the child's call returns. At server-OnEnd we
	// varint-encode the slice (dropping the last entry per simulator
	// semantics; the last sibling's end is implicit at trace
	// reconstruction) and stash the bytes on a per-span attribute that
	// the SB processor reads at its own OnEnd hook.
	childrenMutex := sync.Mutex{}
	endEvents := []int(nil)
	ctx = context.WithValue(ctx, "endEvents", &endEvents)
	ctx = context.WithValue(ctx, "childrenMutex", &childrenMutex)

	err = handler.Service.UnfollowWithUsername(ctx, reqID, userUsername, followeeUsername)
	if err != nil {
		span.RecordError(err)
	}

	span.SetAttributes(attribute.Int("eventCount", int(eventCount.Load())))
	// Encode the end-event seqs as varint bytes for the SB processor.
	// Format: varint(count) || count*varint(seq). The SB processor's
	// OnEnd hook prepends traceID+depth to form a full DEE triple.
	// Drop the last entry — its end is implicit at reconstruction time
	// (mirrors the simulator's kept = rem[:len(rem)-1]).
	if n := len(endEvents); n > 0 {
		kept := endEvents[:n-1]
		buf := make([]byte, 0, 8+5*len(kept))
		buf = binary.AppendUvarint(buf, uint64(len(kept)))
		for _, s := range kept {
			buf = binary.AppendUvarint(buf, uint64(s))
		}
		// Base64 the bytes because OTel attributes don't expose a
		// native []byte type; this is the SDK's only string round
		// trip on the DEE path, paid once per server-OnEnd.
		span.SetAttributes(attribute.String("remEndEvents", base64.RawURLEncoding.EncodeToString(buf)))
	}

	return
}
