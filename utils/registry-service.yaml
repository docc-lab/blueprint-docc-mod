apiVersion: v1
kind: Service
metadata:
  name: docker-registry
  namespace: registry
  labels:
    app: docker-registry
spec:
  type: NodePort
  ports:
  - port: 30000
    targetPort: 5000
    nodePort: 30000
    name: registry
  selector:
    app: docker-registry
